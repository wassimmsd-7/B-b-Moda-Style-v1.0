import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bebemodastyle.app',
  appName: 'Bèbè Moda Style',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
